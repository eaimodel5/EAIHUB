import React from 'react';
import ChatInterface from '../components/ChatInterface';

const StudentStudio: React.FC = () => {
  return <ChatInterface impact={impact} />;
};

export default StudentStudio;