# Lovable upload notes

Upload this zip to Lovable. Use the prompt in `LOVABLE_PROMPT.txt`.

Use `/src` as the codebase. Ignore `_sources/` (it is just a reference snapshot).

Supabase:
- Apply `/supabase/schema.sql` in your Supabase project.
- Set env vars in Lovable:
  - VITE_SUPABASE_URL
  - VITE_SUPABASE_ANON_KEY

